# Plan: seguridad de dinero, tarifas por distancia, GPS en paradas/abordaje, y limpieza de huérfanos

> Estado: **propuesto, sin ejecutar todavía** — el usuario pidió leerlo con calma antes de tocar código. Ver también el artifact con el diagnóstico completo de Padre/Homúnculos y las decisiones tomadas.

## Contexto

Tras revisar el Figma de los 5 perfiles, el usuario pidió una lista de 11 cambios (parada por GPS, push real, nuevo modelo de pago con aviso de bajada, panel de tarifas, limpieza de huérfanos/duplicados, un bug de "ruta asignada", QR de recarga editable, verificación de recargas por el tickeador, compresión de imágenes, seguimiento de cámara en tiempo real, y desvíos de ruta). Se convocó a Padre con sus Homúnculos para triage — verificaron cada punto contra el código real (no especulación) y encontraron algo más urgente que la lista original: **la capa de dinero tiene agujeros activos hoy** (recargas que se auto-aprueban con cualquier monto/imagen, un mismo QR de cobro que puede cobrarse dos veces, y el saldo del chofer que se cuenta doble). El usuario ya decidió:
- Aceptar que la capa de dinero siga siendo "cliente-confiable" por ahora (sin Cloud Functions) — arreglar los bugs obvios dentro del modelo actual.
- Si el pasajero no avisa que bajó, se le cobra la tarifa máxima (5 Bs).
- El monto libre que el chofer teclea al cobrar por QR y la tarifa oficial del presidente **conviven** (no se reemplaza uno por otro).
- Los desvíos de ruta: además del mapa al tocar una card, el presidente puede marcar una nota simple de "calle bloqueada" (sin recálculo automático de ruta alterna — no es viable sin servidor de ruteo propio).

Quedan fuera de alcance por ahora (requieren decisión de infraestructura que el usuario no ha tomado): push real vía FCM (exige plan Blaze) y ruteo alterno automático tipo Google (exige servidor de ruteo propio). El bug "la ruta asignada carga todas las rutas" no se pudo reproducir en el código actual — se investigará en vivo durante la verificación final en el celular en vez de a ciegas.

## Bloque 0 — Seguridad del dinero (primero, sin excepción)

**Quitar la auto-aprobación de recargas.** `lib/features/user/presentation/bloc/recharge_bloc.dart:34` llama `approveRecharge` inmediatamente después de crear la solicitud — se borra esa línea. La recarga queda en `pending` hasta que el tickeador la verifique (ver Bloque 2).

**Evitar el doble cobro del mismo QR.** `lib/features/user/domain/services/trip_payment_service.dart`, dentro de `processPayment`: hoy lee el trip fuera de la transacción (línea 31) y nunca revalida `payment_status` dentro de `runTransaction` (63-84). Se cambia para que el propio `runTransaction` haga `transaction.get(tripRef)` como primera operación y lance una excepción si `payment_status` ya es `'paid'`, antes de hacer ningún `update`. Regla de Firestore: todas las lecturas de una transacción van antes que las escrituras.

**Eliminar el doble conteo del saldo del chofer.** `lib/features/user/data/datasources/wallet_datasource.dart`, método `getWalletByUserId` (líneas 61-77): quitar la suma de `_sumDriverEarnings(userId)` sobre `current_balance` — `TripPaymentService.processPayment` ya incrementa `wallet.current_balance` del chofer dentro de la misma transacción atómica (líneas 71-74 de `trip_payment_service.dart`), así que sumarlo de nuevo en la lectura duplica el monto. `getDriverEarningsTransactions` NO se toca — sigue usándose correctamente en otros lados solo para listar transacciones (`GananciasChoferPage`, `DriverWalletPage`), no para calcular saldo.

## Bloque 1 — Cambios baratos e independientes

**Cámara del chofer sin seguimiento.** `lib/features/driver/presentation/widgets/driver_service_map.dart`, método `_startLiveTracking` (72-82): agregar `_controller?.animateCamera(CameraUpdate.newLatLng(...))` dentro del listener de `getPositionStream`, junto al `setState` que ya mueve el marcador. Es el único archivo con este patrón en todo el proyecto (confirmado — `ruta_navegacion_page.dart` ya implementa el seguimiento completo del lado del pasajero, no hace falta tocarlo).

**Compresión de imágenes al subir.** Los 7 call sites de `ImagePicker().pickImage(...)` (`solicitud_chofer_page.dart`, `editar_perfil_page.dart`, `recarga_qr_page.dart`, `subir_fotografia_page.dart` [se borra, ver huérfanos], `pago_qr_page.dart`, `solicitud_beneficio_page.dart`) usan `imageQuality` entre 80-100 sin criterio y sin `maxWidth`/`maxHeight`. Se normaliza a `imageQuality: 80, maxWidth: 1280` en cada uno — sin agregar dependencia nueva, `image_picker` ya soporta esos parámetros.

**Borrado de huérfanos y duplicados confirmados** (cero referencias de navegación reales en `lib/`, verificado por Padre):
- `lib/features/user/presentation/pages/{movimientos_page,confirmacion_recarga_page,maps_page,rutas_sugerencias_page,subir_fotografia_page,ruta_tiempo_page,ruta_abordaje_page}.dart`
- `lib/features/driver/presentation/pages/tickeador_operation_register_page.dart` (y su cadena bloc/datasource asociada, ya marcada en `docs/DEUDA_TECNICA.md`)
- La cadena `DriverAssignedRoutesDatasource`/su bloc (NO confundir con `DriverAssignedRoutesPage`, que sí está viva y correcta) — quitar también su registro en `lib/core/di/dependency_injection.dart:647-657`.
- `NearbyStopsBloc` (no `BusStopService`, que sigue vivo vía `parada_detalle_page.dart`).
- `RechargeService.getQRCodeUrl` (genera un QR externo vía `api.qrserver.com`, sin ningún caller) — se borra el método.
- Al borrar cada página, quitar también sus imports huérfanos en los archivos que alguna vez los referenciaron (ej. `movimientos_page.dart` en `wallet_page.dart`, ya con import residual).

**UI de admin para el QR de recarga.** El QR ya es dinámico (`config/qr_recarga.qr_url` en Firestore, leído por `recarga_qr_page.dart:64-80`) — solo falta la pantalla. Nueva página en `lib/features/admin/presentation/pages/actualizar_qr_recarga_page.dart`: sube una imagen vía `StorageService` (nuevo método corto, mismo patrón que `uploadVehicleDocument`, ruta `config/qr_recarga.jpg`) y escribe `config/qr_recarga.qr_url` con el resultado. Entrada desde `admin_home_page.dart` (mismo patrón de menú que "Asignar línea a presidente"). `firestore.rules:118-121` ya permite `allow write: if isAdmin()` sobre `config` — no requiere cambio de reglas.

**Activar el seed de paradas + parada por GPS.** `lib/core/local_db/route_local_database.dart`: `_ensureDataReady()` (64-78) no llama `_seedStopsFromGtfs()` (167-172, ya completo) — se agrega la llamada. `getStopsNearPoint` (287) ya existe y `stops_meta.route_refs` ya se puebla desde `stop_desc` (`gtfs_datasource.dart:177`). En `lib/features/driver/presentation/pages/driver_rutas_page.dart`, reemplazar el `TextField` de nombre libre en `_NotifyStopSection` (~línea 289) por: escuchar la posición del chofer (ya disponible vía `DriverServiceMap`/stream de ubicación) y filtrar `getStopsNearPoint` por `route_refs` conteniendo la línea asignada, mostrando la parada más cercana detectada en vez de pedir texto. Revisar primero que `stop_desc` del GTFS de Cochabamba realmente traiga refs de línea usables (no asumido, Padre no lo verificó) — si no trae datos utilizables, usar cercanía pura sin filtrar por línea como fallback documentado, no bloquear la tarea por esto.

**Cards de ruta del presidente → mapa + desvío manual.** `lib/features/presidente/presentation/pages/presidente_panel_page.dart`, `_RouteControlSection`: agregar `onTap` a cada card que navegue a una vista de mapa reusando `AssignedRouteCard` (`driver_assigned_routes_page.dart:105`, ya dibuja ruta+calles con `TripRouteMap`). En esa vista, agregar un botón "Marcar desvío" que abre un diálogo simple (texto libre: qué calle/tramo está bloqueado) y lo guarda como una nota fechada — **no** en el documento `routes` (que se re-siembra desde GTFS y es `allow write: if isAdmin()`), sino en una colección nueva `route_deviation_notes/{autoId}` (`route_ref`, `note`, `reported_by`, `created_at`, `active`), documentada en `FIRESTORE_COLLECTIONS_GUIDE.md`. Regla nueva: `allow read: if isSignedIn(); allow write: if isStaffManager();` (mismo criterio ya usado para reclamos/vehículos). Es informativo únicamente — no recalcula ninguna ruta.

## Bloque 2 — Cadena de pago (en este orden estricto, cada paso depende del anterior)

**1. Reconciliar qué línea paga cada viaje.** `lib/features/driver/domain/services/driver_service.dart`, método `generateTripCharge` (171-188): cambia `routeRef: vehicle.lineNumber` por `routeRef: route?.ref ?? vehicle.lineNumber` — el caller (`DriverOperationsBloc._onGenerateCharge`) ya resuelve y pasa `route: current.assignedRoute`, que es la fuente de verdad correcta según el propio comentario de `getAssignedRoute` (líneas 156-166 del mismo archivo). Sin este cambio, las tarifas por línea del siguiente paso nacerían aplicándose a la línea equivocada quienes tengan `assigned_route_ref` distinto de `vehicle.lineNumber`.

**2. Panel de tarifas del presidente.** Colección nueva `tariffs/{route_ref}` (el ref de línea como ID del documento, no un campo — así la regla de Firestore puede exigir "solo tu línea" de forma nativa): campo `brackets` como array de `{max_km: number|null, fare: number}` ordenado ascendente (ej. `[{max_km:10, fare:3}, {max_km:15, fare:4}, {max_km:null, fare:5}]`, `max_km:null` = "mayor a los anteriores"). Se documenta en `FIRESTORE_COLLECTIONS_GUIDE.md`. Regla: `allow read: if isSignedIn(); allow write: if isAdmin() || (isSignedIn() && requester().get('role','user') == 'presidente' && route_ref in requester().get('presidente_info', {}).get('managed_lines', []));` — sigue el mismo patrón ya usado para `presidente_info.managed_lines` (agregado en sesión anterior). Nueva página `lib/features/presidente/presentation/pages/tarifas_page.dart` (formulario simple: 3 campos numéricos por bracket), entrada desde `presidente_panel_page.dart` junto a "Calificación de choferes"/"Reclamos". Nuevo `TariffDatasource`/`TariffService` en `lib/features/routes/` (mismo patrón CRUD simple que `ClaimDatasource`), con una función pura `calculateFareForDistance(List<FareBracket> brackets, double km)`.

**3. Aviso de bajada + cobro por distancia.** Reusa el mismo doc `trips` que ya crea el chofer (**no** una colección nueva — 5 lectores existentes asumen "1 doc = 1 cobro", ej. `completedTrips` en `operational_report_datasource.dart:150` cuenta `driverTrips.length`; duplicar documentos por pasajero rompería ese conteo). Cambios:
   - `passenger_id` se escribe **al abordar** (nuevo evento explícito, no al pagar) — reemplaza el heurístico de "pagó en las últimas 3 horas" de `getRecentPassengerIdsForVehicle` (`driver_datasource.dart:286-302`), que ya no hace falta cuando el abordaje se registra directo.
   - Nuevo botón "Aviso de bajada" en la pantalla de navegación del pasajero (`ruta_navegacion_page.dart`, junto al flujo ya existente de `TripPhaseService`/`NavigationBloc`, que ya detecta la posición en tiempo real): calcula la distancia recorrida con la función ya existente de proyección punto-a-segmento en `DistanceUtils` (34-52) sobre el `RouteEntity.polyline` del trip, resuelve la tarifa vía `TariffService.calculateFareForDistance` usando `tariffs/{trip.route_ref}` (ya reconciliado en el paso 1), y llama a `TripPaymentService` (mismo servicio que ya usa el flujo de QR) para completar el pago — sin inventar un segundo mecanismo de cobro, solo un segundo punto de entrada hacia el mismo servicio.
   - Si el pasajero nunca avisa que bajó y el chofer detiene servicio o el trip queda `pending` más de un umbral razonable (a definir en implementación, ej. 2 horas), se cobra automáticamente la tarifa máxima del bracket (`fare` del último bracket) — política ya decidida por el usuario.
   - Elimina el mecanismo fantasma de `ruta_navegacion_page.dart:175-182` (guardaba `farePaid: 2.5` hardcodeado en el historial personal del pasajero sin tocar `trips` ni la billetera real) — se reemplaza por la llamada real a `TripPaymentService` de arriba. Corrige también los otros usos del `2.5` hardcodeado como estimación de tarifa en planificación de viaje (`planned_trip.dart:124`, mostrado en `plan_detalle_page.dart`/`planificar_viaje_page.dart` y en el PDF `cancelled_trips_pdf_service.dart:99`) para que consulten `TariffService` en vez de un número fijo — son estimaciones de planificación, no cobros reales, pero mostrar/imprimir un monto inventado en un PDF de recibo es la exposición más seria que Padre encontró fuera del alcance original.

## Verificación

- `flutter analyze` tras cada bloque — 0 errores antes de seguir al siguiente.
- Bloque 0: probar manualmente que (a) una recarga nueva queda en estado pendiente, no se acredita sola; (b) generar un cobro QR y "pagarlo" dos veces seguidas (dos llamadas a `processPayment` con el mismo `tripId`) falla la segunda vez; (c) el saldo mostrado al chofer coincide con la suma real de sus `trip_payment_received`, sin duplicar.
- Bloque 1: instalar en el Mi A3 y confirmar en vivo — la cámara del chofer seguía la posición al moverse, las 8 páginas borradas no dejan imports rotos (`flutter analyze` ya lo cubre), el admin puede subir un nuevo QR de recarga y se refleja en `RecargaQrPage`, y — clave para cerrar el punto pendiente — reproducir en el celular real el bug de "carga todas las rutas" para identificar la pantalla exacta si sigue apareciendo.
- Bloque 2: flujo completo de punta a punta en el celular — chofer asigna/verifica su línea, presidente configura tarifas para esa línea, pasajero viaja, avisa que baja, se le cobra la tarifa correcta según distancia real, el chofer ve el ingreso reflejado, y el PDF de un viaje planificado ya no muestra `2.5` fijo.
- Actualizar `FIRESTORE_COLLECTIONS_GUIDE.md` con las colecciones nuevas (`tariffs`, `route_deviation_notes`) según se implementen, y `docs/DEUDA_TECNICA.md` retirando las entradas que queden resueltas por la limpieza de huérfanos.
